# Функционал бота shftsecurebot

## Общая информация

shftsecurebot — Telegram-бот для управления панелью Remnawave и продажи подписок на сервис защищённого интернет-соединения shftsecure через Telegram Stars. Бот предоставляет публичный интерфейс для пользователей и административную панель для управления инфраструктурой.

**Локализация:** Русский (ru) и Английский (en)

---

## 📱 ПУБЛИЧНЫЙ ФУНКЦИОНАЛ (для обычных пользователей)

### Главное меню пользователя

#### 1️⃣ Подключить доступ (`/start`)
- **Покупка подписки** — выбор периода (1, 3, 6, 12 месяцев) и оплата через Telegram Stars
- **Пробная подписка** — бесплатная активация на 3 дня (один раз)

#### 2️⃣ Мой доступ
- Просмотр статуса подписки (ACTIVE, DISABLED, LIMITED, EXPIRED)
- Дата истечения подписки
- Использованный/доступный трафик
- Ссылка на конфигурацию (subscription URL)

#### 3️⃣ Настройки
- **Язык интерфейса** — выбор между русским и английским
- **Автопродление** — включение/выключение напоминаний об истекающей подписке
  - При включении: бот отправляет напоминания заранее и создает invoice для быстрого продления
  - Напоминания отправляются за 3-5 дней, за 1 день и после окончания подписки
  - Оплата происходит только после подтверждения пользователем (не автоматически)
- **Реферальная программа** — просмотр реферальной ссылки, количества рефералов и начисленных бонусных дней
- **Документы**:
  - Политика конфиденциальности
  - Публичная оферта
  - Правила использования сервиса

#### 4️⃣ Поддержка
- Информация о поддержке

---

## 💳 СИСТЕМА ПЛАТЕЖЕЙ

### Покупка подписки
- **Варианты периодов:** 1, 3, 6, 12 месяцев
- **Валюта:** Telegram Stars (XTR)
- **Цены настраиваются** через переменные окружения:
  - `SUBSCRIPTION_STARS_1MONTH`
  - `SUBSCRIPTION_STARS_3MONTHS`
  - `SUBSCRIPTION_STARS_6MONTHS`
  - `SUBSCRIPTION_STARS_12MONTHS`

### Промокоды
- Ввод промокода при покупке (опционально)
- Типы промокодов:
  - Скидка в процентах (`discount_percent`)
  - Бонусные дни (`bonus_days`)
- Ограничения:
  - Максимальное количество использований (`max_uses`)
  - Срок действия (`expires_at`)

### Пробная подписка (Trial)
- Одноразовая активация на 3 дня (настраивается через `TRIAL_DAYS`)
- Автоматическое создание пользователя в Remnawave
- Применение дефолтных сквадов (external и internal)

### Обработка платежей
- Pre-checkout проверка перед оплатой
- Автоматическое создание/обновление пользователя в Remnawave после успешной оплаты
- Продление существующей подписки (от текущей даты истечения)
- Автоматическое начисление реферального бонуса

### Автопродление (Auto-renewal)
- **Включение/выключение** в настройках пользователя
- **Напоминания**:
  - За 3-5 дней до окончания подписки — напоминание с кнопкой "Продлить доступ"
  - За 1 день до окончания — повторное напоминание
  - После окончания подписки — сообщение о приостановке доступа с кнопкой "Возобновить доступ"
- **Продление доступа**:
  - Кнопка "Продлить доступ" создает invoice для оплаты (1 месяц по умолчанию)
  - Telegram открывает платеж автоматически
  - Пользователь подтверждает оплату Stars
  - Оплата происходит только после подтверждения (не автоматически)
- **Фоновая проверка**:
  - Автоматическая проверка истекающих подписок каждые 6 часов
  - Отправка напоминаний пользователям с включенным автопродлением
- **База данных**:
  - Хранение статуса автопродления (`auto_renewal`)
  - Хранение времени последнего напоминания (`last_renewal_notification`)

---

## 🎁 РЕФЕРАЛЬНАЯ ПРОГРАММА

- Реферальная ссылка: `https://t.me/{bot_username}?start={user_id}`
- Начисление бонусных дней рефереру при:
  - Активации пробной подписки рефералом
  - Покупке подписки рефералом
- Количество бонусных дней настраивается через `REFERRAL_BONUS_DAYS` (по умолчанию 3 дня)
- Бонус начисляется только один раз за каждого реферала
- Уведомления рефереру о начислении бонуса

---

## 🔐 АДМИН-ПАНЕЛЬ (для администраторов)

### Главное меню админа

#### 📊 Статистика системы
- Общее количество пользователей
- Онлайн пользователей (в реальном времени)
- Количество нод (всего / включено / онлайн)
- Количество хостов (всего / включено)

---

## 👥 УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ

### Поиск пользователей
- Поиск по:
  - Username
  - Email
  - Telegram ID
  - Description

### Создание пользователя
- Команда: `/user_create`
- Интерактивный режим или параметры:
  - Username
  - Дата истечения (ISO формат)
  - Telegram ID (опционально)

### Действия с пользователем
- Просмотр детальной информации
- **Включить/Выключить** (Enable/Disable)
- **Сброс трафика** (Reset traffic)
- **Отозвать подписку** (Revoke subscription)
- **Просмотр конфигураций** пользователя
- **Редактирование** пользователя
- **Статистика** пользователя
- **Трафик по нодам** (распределение трафика)
- **HWID устройства** (устройства пользователя)

### Список подписок
- Пагинация списка пользователей
- Переход к детальной информации о пользователе

---

## 🛰 УПРАВЛЕНИЕ НОДАМИ

### Команды
- `/nodes` — список всех нод
- `/node <uuid>` — детальная информация о ноде
- `/nodes_usage` — статистика использования в реальном времени
- `/nodes_range <start_iso> <end_iso>` — трафик за период

### Действия с нодой
- Просмотр детальной информации
- Редактирование параметров ноды
- Включение/выключение ноды
- Создание новой ноды

---

## 🖥 УПРАВЛЕНИЕ ХОСТАМИ

### Команды
- `/hosts` — список всех хостов
- `/host <uuid>` — детальная информация о хосте

### Действия с хостом
- Просмотр детальной информации
- Редактирование параметров хоста
- Включение/выключение хоста
- Создание нового хоста

---

## 🧰 РЕСУРСЫ

### API Токены
- `/tokens` — список всех API токенов
- `/token <name>` — создание нового токена

### Шаблоны (Templates)
- `/templates` — список всех шаблонов
- `/template <uuid>` — детальная информация о шаблоне
- Создание нового шаблона
- Редактирование шаблона
- Редактирование JSON шаблона
- Изменение порядка шаблонов

### Сниппеты (Snippets)
- `/snippets` — список всех сниппетов
- `/snippet <name>` — детальная информация о сниппете
- `/snippet_add` — создание нового сниппета
- `/snippet_update` — обновление существующего сниппета

### Конфигурации (Configs)
- `/configs` — список всех конфигураций
- `/config <uuid>` — детальная информация о конфигурации

---

## 💳 БИЛЛИНГ

### Команды
- `/billing` — общая информация о биллинге
- `/billing_nodes` — биллинг по нодам
- `/providers` — список провайдеров

### Функции
- Просмотр статистики биллинга
- Фильтрация по провайдерам
- Просмотр истории платежей
- Анализ по нодам

---

## 📦 МАССОВЫЕ ОПЕРАЦИИ (Bulk Actions)

### Команды
- `/bulk` — меню массовых операций
- `/bulk_delete <uuid1> <uuid2> ...` — удаление нескольких пользователей
- `/bulk_revoke <uuid1> <uuid2> ...` — отзыв подписок у нескольких пользователей
- `/bulk_reset <uuid1> <uuid2> ...` — сброс трафика у нескольких пользователей
- `/bulk_extend <days> <uuid1> <uuid2> ...` — продление подписки на N дней для нескольких пользователей
- `/bulk_extend_all <days>` — продление подписки на N дней для всех пользователей
- `/bulk_status <status> <uuid1> <uuid2> ...` — изменение статуса нескольких пользователей
- `/bulk_delete_status <status>` — удаление всех пользователей с определенным статусом

### Доступные статусы
- ACTIVE
- DISABLED
- LIMITED
- EXPIRED

---

## ⚙️ СИСТЕМА

### Команды
- `/health` — проверка здоровья системы (API статус)
- `/stats` — общая статистика
- `/bandwidth` — информация о трафике системы
- `/help` — справка по командам

### Функции
- Мониторинг состояния API
- Просмотр системной статистики
- Анализ трафика

---

## 🔔 УВЕДОМЛЕНИЯ

### Каналы уведомлений
- Настраиваются через `NOTIFICATIONS_CHAT_ID` и `NOTIFICATIONS_TOPIC_ID`
- Уведомления отправляются в указанный чат/топик

### Типы уведомлений
1. **Активация пробной подписки**
   - User ID, Username
   - Количество дней триала
   - UUID пользователя в Remnawave

2. **Успешная оплата**
   - User ID, Username
   - Период подписки (месяцы)
   - Сумма в Stars
   - Промокод (если был использован)
   - UUID пользователя
   - Дата истечения

3. **Использование промокода**
   - User ID, Username
   - Код промокода
   - Тип скидки/бонуса

4. **Реферальный бонус**
   - ID и Username реферера
   - ID и Username реферала
   - Количество бонусных дней
   - Новая дата истечения подписки реферера

---

## 🗄️ БАЗА ДАННЫХ

### Таблицы SQLite

1. **bot_users** — пользователи бота
   - telegram_id (PRIMARY KEY)
   - username
   - language (ru/en)
   - registered_at
   - trial_used (boolean)
   - referrer_id (ссылка на другого пользователя)
   - remnawave_user_uuid (UUID в Remnawave)

2. **promo_codes** — промокоды
   - code (PRIMARY KEY)
   - discount_percent
   - bonus_days
   - max_uses
   - current_uses
   - expires_at
   - created_at
   - is_active

3. **promo_code_usage** — история использования промокодов
   - id
   - code
   - user_id
   - used_at

4. **referrals** — реферальные связи
   - id
   - referrer_id
   - referred_id
   - bonus_days
   - registered_at

5. **payments** — платежи
   - id
   - user_id
   - stars (сумма в Stars)
   - status (pending/completed/failed)
   - remnawave_user_uuid
   - invoice_payload
   - subscription_days
   - promo_code
   - created_at
   - completed_at

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Интеграция с Remnawave API
- Автоматическое создание пользователей при оплате/триале
- Применение дефолтных сквадов (`DEFAULT_EXTERNAL_SQUAD_UUID`, `DEFAULT_INTERNAL_SQUADS`)
- Продление подписки для существующих пользователей
- Получение конфигурационных ссылок

### Middleware
- **AdminMiddleware** — проверка прав администратора (блокирует неавторизованных)
- **i18n Middleware** — автоматическая локализация сообщений

### Обработка ошибок
- Централизованная обработка ошибок API
- Обработка ошибок платежей
- Логирование всех критических ошибок

### Безопасность
- Проверка прав администратора на всех админских командах
- Валидация платежей (pre-checkout)
- Проверка суммы платежа
- Защита от повторной обработки платежей

---

## 📋 КОМАНДЫ АДМИНА

### Основные
- `/start` — приветствие (доступно всем)
- `/help` — справка по командам
- `/health` — проверка здоровья системы
- `/stats` — статистика
- `/bandwidth` — информация о трафике

### Пользователи
- `/user <query>` — поиск пользователя
- `/user_create [username] [expire_at] [telegram_id]` — создание пользователя
- `/sub <short_uuid>` — информация о подписке

### Ноды и хосты
- `/nodes` — список нод
- `/node <uuid>` — детали ноды
- `/nodes_usage` — статистика использования
- `/nodes_range <start> <end>` — трафик за период
- `/hosts` — список хостов
- `/host <uuid>` — детали хоста

### Ресурсы
- `/tokens` — список токенов
- `/token <name>` — создание токена
- `/templates` — список шаблонов
- `/template <uuid>` — детали шаблона
- `/snippets` — список сниппетов
- `/snippet <name>` — детали сниппета
- `/snippet_add` — создание сниппета
- `/snippet_update` — обновление сниппета
- `/configs` — список конфигураций
- `/config <uuid>` — детали конфигурации

### Биллинг
- `/billing` — информация о биллинге
- `/billing_nodes` — биллинг по нодам
- `/providers` — провайдеры

### Массовые операции
- `/bulk` — меню массовых операций
- `/bulk_delete <uuids...>` — удаление пользователей
- `/bulk_revoke <uuids...>` — отзыв подписок
- `/bulk_reset <uuids...>` — сброс трафика
- `/bulk_extend <days> <uuids...>` — продление подписки
- `/bulk_extend_all <days>` — продление всем
- `/bulk_status <status> <uuids...>` — изменение статуса
- `/bulk_delete_status <status>` — удаление по статусу

---

## 🎨 ИНТЕРФЕЙС

### Клавиатуры (Inline Keyboards)
- Все действия выполняются через кнопки (callback queries)
- Навигация по меню с помощью кнопок "Назад"
- Пагинация для длинных списков
- Контекстные действия для каждого объекта (пользователь, нода, хост и т.д.)

### Форматирование
- Markdown для админ-панели
- HTML для публичных сообщений пользователям
- Красивое форматирование чисел (трафик, даты)
- Эмодзи для лучшей навигации

---

## 📝 ЗАМЕТКИ

- Все цены настраиваются через переменные окружения
- Дефолтные сквады настраиваются через `DEFAULT_EXTERNAL_SQUAD_UUID` и `DEFAULT_INTERNAL_SQUADS`
- Бот автоматически проверяет подключение к API при запуске
- При недоступности API бот не запускается
- База данных SQLite хранится в файле `bot_data.db` в корне проекта


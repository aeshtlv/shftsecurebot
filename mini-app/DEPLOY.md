# 🚀 Деплой Mini App на shftsecure.one

## Предварительные требования

1. **Сервер** с Docker и Docker Compose
2. **Домен** shftsecure.one с доступом к DNS
3. **Открытые порты**: 80, 443

---

## Шаг 1: Настройка DNS

Добавьте A-запись в DNS вашего домена:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | app | IP_ВАШЕГО_СЕРВЕРА | 300 |

Пример для shftsecure.one:
```
app.shftsecure.one → 123.45.67.89
```

**Проверка DNS (подождите 5-10 минут после добавления):**
```bash
nslookup app.shftsecure.one
# или
dig app.shftsecure.one
```

---

## Шаг 2: Загрузка на сервер

```bash
# На сервере
cd /opt
git clone <ваш-репо> shftsecure-miniapp
# или скопируйте папку mini-app/

cd shftsecure-miniapp/mini-app
```

---

## Шаг 3: Сборка фронтенда

```bash
cd frontend

# Установка зависимостей
npm ci

# Сборка для продакшена
npm run build

# Проверка - должна появиться папка dist/
ls -la dist/
```

---

## Шаг 4: Запуск Caddy

```bash
cd ..  # вернуться в mini-app/

# Запуск
docker compose up -d caddy

# Проверка статуса
docker compose ps

# Логи (Caddy покажет получение сертификата)
docker compose logs -f caddy
```

**Вы должны увидеть:**
```
caddy  | {"level":"info","msg":"certificate obtained successfully"}
caddy  | {"level":"info","msg":"serving initial configuration"}
```

---

## Шаг 5: Проверка

Откройте в браузере:
```
https://app.shftsecure.one
```

Должен открыться Mini App с SSL (🔒 в адресной строке).

---

## Шаг 6: Настройка в BotFather

1. Откройте @BotFather в Telegram
2. Выберите вашего бота
3. **Bot Settings** → **Menu Button** → **Configure menu button**
4. Введите:
   - **URL:** `https://app.shftsecure.one`
   - **Title:** `Панель управления`

Теперь в боте появится кнопка "Панель управления" рядом с полем ввода!

---

## Команды управления

```bash
# Перезапуск
docker compose restart caddy

# Остановка
docker compose down

# Логи
docker compose logs -f caddy

# Пересборка фронтенда и перезапуск
cd frontend && npm run build && cd .. && docker compose restart caddy
```

---

## Обновление Mini App

```bash
# 1. Обновить код
git pull

# 2. Пересобрать фронтенд
cd frontend
npm ci
npm run build
cd ..

# 3. Caddy автоматически подхватит новые файлы
# (или перезапустите для надёжности)
docker compose restart caddy
```

---

## Troubleshooting

### Сертификат не получен
```bash
# Проверьте DNS
nslookup app.shftsecure.one

# Проверьте порты
sudo netstat -tlnp | grep -E ':(80|443)'

# Логи Caddy
docker compose logs caddy | grep -i "certificate\|error"
```

### 502 Bad Gateway
```bash
# Проверьте, что dist/ существует
ls -la frontend/dist/

# Перезапустите
docker compose restart caddy
```

### Страница не обновляется
```bash
# Очистите кэш браузера или откройте в инкогнито
# Ctrl+Shift+R (жёсткое обновление)
```

---

## Структура после деплоя

```
/opt/shftsecure-miniapp/mini-app/
├── Caddyfile              ← Конфигурация веб-сервера
├── docker-compose.yml     ← Docker конфигурация
├── frontend/
│   ├── dist/              ← Собранный фронтенд (раздаётся Caddy)
│   ├── src/               ← Исходники
│   └── package.json
└── backend/               ← API (опционально)
```

